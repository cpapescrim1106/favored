# Project Context

## Remote Development Environment
- User is SSH'd into a remote machine
- `localhost` URLs are not accessible to the user
- Use Tailscale for network connections when needed

## Verification
- Use Playwright MCP to verify UI changes when appropriate
- Test visual changes in the browser before considering work complete
